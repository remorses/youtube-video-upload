

local_server: true # use a local server instead of a url to get credentials
                   # this is necessary when using this script from localhost

videos:
    -
        title:       ima little bitch!
        file:        tests/video.mp4
        description: esghere
        category:    Music
        privacy:     private
        tags:
            - holy
            - shit

secrets_path:        tests/client_secrets.json
credentials_path:    tests/credentials.json
